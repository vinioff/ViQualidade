{
  "name": "vi-qualidade-api",
  "version": "1.0.0",
  "description": "API para gerenciar encomendas do Vi Qualidade",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}